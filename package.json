{
  "name": "student-portal-api",
  "version": "1.0.0",
  "description": "First Express App - Student Portal API (JSON only, no frontend)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
